export const initialState = {
    smurfs: [],
    isFetching: false,
    isUpdating: false,
    isDeleting: false
}

export const smurfsReducers = (state = initialState, action) => {
    switch(action.type){

        default:
            return state
    }
    
}